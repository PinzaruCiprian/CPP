# Configuration file for Behave

[behave]
# Path to feature files
paths = features

# Show stdout/stderr
stdout_capture = False
stderr_capture = False

# Logging
logging_level = INFO
log_capture = True

# Format: plain (simplu, evita encoding issues)
format = plain

# Implicit wait pentru Selenium
default_wait = 10
