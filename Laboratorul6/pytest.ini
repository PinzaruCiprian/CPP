"""
Pytest configuration file
"""

import pytest
import sys
from pathlib import Path

# Add project root to path
project_root = Path(__file__).parent
sys.path.insert(0, str(project_root))


def pytest_configure(config):
    """Configurare pytest"""
    config.addinivalue_line(
        "markers", "smoke: mark test as smoke test"
    )
    config.addinivalue_line(
        "markers", "regression: mark test as regression test"
    )
    config.addinivalue_line(
        "markers", "validation: mark test as validation test"
    )


# Optional: Fixtures pentru testare
@pytest.fixture(scope="session")
def setup_driver():
    """Setup WebDriver pentru testare"""
    from utils.driver_factory import WebDriverFactory
    driver = WebDriverFactory.create_driver()
    yield driver
    WebDriverFactory.close_driver()
